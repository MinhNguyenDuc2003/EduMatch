version: '3.8'

services:
  scholarship-api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - huggingface_cache:/root/.cache/huggingface
    environment:
      - HF_HOME=/root/.cache/huggingface

volumes:
  huggingface_cache:
